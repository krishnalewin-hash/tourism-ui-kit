{
  "name": "tourism-api-cloudflare",
  "version": "1.0.0",
  "description": "Cloudflare Workers API for tourism data",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy:staging": "wrangler deploy --env staging",
    "deploy:prod": "wrangler deploy --env production",
    "migrate": "node scripts/migrate-from-sheets.js"
  },
  "keywords": ["cloudflare", "workers", "tourism", "api"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "wrangler": "^3.0.0"
  },
  "dependencies": {
    "node-fetch": "^2.6.7"
  }
}

