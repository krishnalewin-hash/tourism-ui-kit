{
  "name": "tourism-cloudflare-api",
  "version": "2.0.0",
  "description": "Cloudflare Workers API for Tourism Platform with Admin Backend",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}

